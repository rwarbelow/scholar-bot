<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#"><b>Scholar Bot</b></a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Go to... <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#">Calendar</a></li>
            <li><a href="#">Facebook</a></li>
            <% if current_teacher? %>
              <li><%= link_to "Teacher Dashboard", teachers_root_path%></li>
            <% end %>
            <% if current_admin? %>
              <li><%= link_to "Download Student Roster", admin_download_students_path%></li>
              <li><%= link_to "Admin Dashboard", admin_root_path%></li>
            <% end %>
            <li class="divider"></li>
            <li><a href="#">One more separated link</a></li>
          </ul>
        </li>
      </ul>
      <% if current_admin? %>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <%= select_tag :student_id, options_from_collection_for_select(all_students, :id, :full_name), { include_blank: true, class: 'chosen-select' } %>
        </div>
        <button type="submit" class="btn btn-sm btn-default">Submit</button>
      </form>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if current_user %>
            <li><%= link_to "Logout", logout_path, :method => "delete" %></li>
          <% else %>
            <a data-toggle="modal" data-target="#myModal"><b>Login</b></a></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
